// Service module containing API calls for login, logout, and token refresh.
// import api from './api';
// import { API_ENDPOINTS } from '../constants/apiEndpoints';

export const authService = {
  login: async (credentials) => {},
  logout: async () => {},
  refreshToken: async () => {},
};
