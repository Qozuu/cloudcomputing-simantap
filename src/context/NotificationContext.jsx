// React Context provider to manage global notification and toast alerts.
import React, { createContext } from 'react';

export const NotificationContext = createContext(null);

export const NotificationProvider = ({ children }) => {
  return null;
};
